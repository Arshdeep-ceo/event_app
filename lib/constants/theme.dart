import 'package:flutter/material.dart';

const Color kprimaryColor = Color(0xffF34150);
const Color ksecondaryColor = Colors.black;
const Color kprimaryColor2 = Color(0xff511911);

const Color kwhite = Colors.white;
const Color kblack = Colors.black;
BorderRadius kborderRadius = BorderRadius.circular(20);

const kscaffoldGradient = BoxDecoration(
  gradient: LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    colors: [
      Color(0xff511911),
      Color(0xff140A08),
    ],
  ),
);
